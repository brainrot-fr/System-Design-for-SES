Simple Object where all of the Murshid's silsila (silsila-e-tarbiyat and silsila-e-faqiri) is stored. Bundled offline with the app. 

The user chooses their murshid during onbarding so theres no API integration hell here.

Kind of similar to how a degree is associated with a student, like *Mr. Syed Jalal, BTech*

They receive bahr-e-aam and urs updates via push notifications (explain how can i achieve this) because in local notifications, the user has to repeatedly open the app so that the local notifications are scheduled and i dont want the app to run as a background service.